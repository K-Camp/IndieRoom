<div class="jumbotron text-center bg-dark text-light py-5">
  <h1 class="display-4 fw-bold">IndieRoom</h1>
  <p class="lead">世界中のインディーゲームを、みんなで広めていこう！</p>
  <hr class="my-4 border-light">
  <%= link_to "今すぐレビューを見る", posts_path, class: "btn btn-primary btn-lg" %>
</div>

<div class="container mt-5">
  <h2 class="text-center mb-4">最近投稿されたゲーム</h2>

  <div class="row justify-content-center">
    <% @latest_games.each do |post| %>
      <div class="col-md-4 mb-4">
        <div class="card h-100 shadow-sm">

          <% if post.game.game_image.attached? %>
            <%= image_tag post.game.game_image, size: "400x400", class: "card-img-top", alt: post.game.game_name %>
          <% else %>
            <%= image_tag "no_image.png", class: "card-img-top", alt: "No image" %>
          <% end %>

          <div class="card-body text-center">
            <h5 class="card-title"><%= post.game.game_name %></h5>
            <%= link_to "詳細を見る", post_path(post), class: "btn btn-primary btn-sm mt-2" %>
          </div>
        </div>
      </div>
    <% end %>
  </div>

  <% if @latest_games.empty? %>
    <p class="text-center text-muted mt-4">まだ投稿されたゲームがありません。</p>
  <% end %>
</div>
